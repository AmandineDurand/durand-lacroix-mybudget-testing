[pytest]

testpaths = tests

python_files = test_*.py

addopts = -v --cov=. --cov-fail-under=80

# Variables d'environnement pour les tests (évite de charger le .env local)
env = DATABASE_URL=postgresql://test_user:test_password@localhost:5432/test_db